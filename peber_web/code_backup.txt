
# Method dengan menggunakan form manual
def add_news_source(request):
    if request.method == 'GET':
    form = News_SourceForm()
    else:
    form = News_SourceForm(request.POST)

    if form.is_valid():
    source_publisher = form.cleaned_data['source_publisher']
    source_category = form.cleaned_data['source_category']
    source_url = form.cleaned_data['source_url']

    news_source = News_Source.objects.create(
    source_publisher = source_publisher,
    source_category = source_category,
    source_url = source_url
    )

    news_source.save()

    peber_web:news_source_detail > Karena mggnakan namespaces di urls.py
    return HttpResponseRedirect(reverse(\
    'peber_web:news_source_detail', kwargs={'news_source_id': news_source.id}))

    return render(request, 'peber_web/add_news_source.html', {'form': form})

